package com.example.projet.repositories;

import com.example.projet.entities.ArchiveSalaire;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ArchiveSalaireRepository extends JpaRepository<ArchiveSalaire,Integer> {
}

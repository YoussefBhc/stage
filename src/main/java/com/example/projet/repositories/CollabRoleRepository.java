package com.example.projet.repositories;

import com.example.projet.entities.CollabRole;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CollabRoleRepository extends JpaRepository<CollabRole,Integer> {
}
